---
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11
build:
  dockerfile: Dockerfile
  requirementsPath: requirements.txt
  useCache: true
run:
  persistenceMount: /data
  scriptName: app.py
  containerPort: 8000
