# Start

```shell
docker run --gpus all -it --rm -v $(pwd):/app/ -w /app tensorflow/tensorflow:cuda bash
```

# Обработка parser data

***Сборка данных о товаре в батчи***: В файле ***```./datasetCreateDescription.py```***

# Пример работы с нейросетью в ***```./test.py```***
